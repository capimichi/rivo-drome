version: '3.8'

services:
  proxy:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${API_HOST_PORT:-8000}:${API_PORT:-8000}"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
